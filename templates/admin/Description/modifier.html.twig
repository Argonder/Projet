{%  extends 'base-admin.html.twig' %}

{% block body %}

        {% for presentation in description %}
                <p>{{ presentation.description }}</p>
        {% endfor %}
                <button class="btn btn-info btn-presentation"> Modifier </button>
        <!-- FORMULAIRE -->
    {{ form_start(form) }}
    {{ form_widget(form) }}
        <button type="submit" class="btn btn-warning" value="publier">Envoyer</button>
    {{ form_end(form) }}
{% endblock %}